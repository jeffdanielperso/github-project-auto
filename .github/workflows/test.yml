name: Test Jeff
on:
  project_card:
    types:
      - moved
jobs:
  test-triggered:
    if: github.event.project_card.column_id == '14432400'
    runs-on: ubuntu-latest
    steps:
      - name: Add label `qa-ready`
        uses: jeffdanielperso/github-project-auto@vlatest
        with:
          add-labels: "qa-ready"